% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotViolin.R
\name{plotViolin}
\alias{plotViolin}
\alias{plotViolin.default}
\alias{plotViolin.formula}
\title{Violin Plot}
\usage{
plotViolin(x, ...)

\method{plotViolin}{default}(
  x,
  ...,
  horizontal = FALSE,
  at = NULL,
  names = NULL,
  add = FALSE,
  bw = "nrd0",
  col = "grey80",
  border = "black",
  lwd = 1,
  box = TRUE,
  grid = NA,
  quantiles = NULL,
  main = "",
  xlab = "",
  ylab = "",
  xlim = NULL,
  ylim = NULL
)

\method{plotViolin}{formula}(
  formula,
  data = NULL,
  subset,
  na.action = na.omit,
  ...,
  horizontal = FALSE,
  at = NULL,
  names = NULL,
  add = FALSE,
  bw = "nrd0",
  col = "grey80",
  border = "black",
  lwd = 1,
  box = TRUE,
  grid = NA,
  quantiles = NULL,
  main = "",
  xlab = "",
  ylab = "",
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{x}{Numeric vector, list of numeric vectors, or first group.}

\item{...}{Additional data vectors (unnamed) or graphical parameters
passed to \code{par()}.}

\item{horizontal}{Logical; if \code{TRUE}, draws horizontal violins.}

\item{at}{Numeric positions of the groups.}

\item{names}{Optional group labels.}

\item{add}{Logical; if \code{TRUE}, adds to an existing plot.}

\item{bw}{Bandwidth specification passed to \code{density()}.}

\item{col}{Fill color(s) of the violins.}

\item{border}{Border color(s) of the violins.}

\item{lwd}{Line width for violin borders.}

\item{box}{Logical or list controlling the boxplot overlay
(see Details).}

\item{grid}{Logical, \code{NA}, or list controlling background grid.}

\item{quantiles}{Optional numeric vector of probabilities for drawing
quantile lines inside each violin.}

\item{main, xlab, ylab}{Plot labels.}

\item{xlim, ylim}{Axis limits.}

\item{formula}{A formula of the form y ~ group.}

\item{data}{Optional data frame.}

\item{subset}{Optional subset expression.}

\item{na.action}{Function to handle missing values.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Draws violin plots for one or more groups, combining kernel density
estimation with optional boxplot overlays. The function follows a
boxplot-like interface and supports both default and formula methods.
}
\details{
The violin shape is constructed from a kernel density estimate of each
group, scaled to a fixed maximum width. Optionally, boxplots and
quantile lines can be added.

Graphical elements such as the boxplot overlay and grid are controlled
via a flexible interface using \code{TRUE}, \code{FALSE}, \code{NA}, or
\code{list(...)} and are evaluated using \code{bedrock::callIf()}.
}
\section{Data Handling}{

The function accepts:
\itemize{
\item a numeric vector
\item multiple vectors via \code{...}
\item a list of numeric vectors
}
Groups are handled similarly to \code{boxplot()}.
}

\examples{
set.seed(1)
x <- rnorm(100)
y <- rnorm(100, 1)

plotViolin(x, y)

# horizontal violins
plotViolin(x, y, horizontal = TRUE)

# with quantiles
plotViolin(x, y, quantiles = c(0.25, 0.5, 0.75))

# custom styling
plotViolin(x, y,
  col = c("lightblue", "salmon"),
  box = list(col = "white"),
  grid = TRUE
)

# formula interface
df <- data.frame(
  value = rnorm(200),
  group = rep(letters[1:4], each = 50)
)

plotViolin(value ~ group, data = df)

}
\seealso{
\code{\link{boxplot}}, \code{\link{density}}

Other plot.univariate:
\code{\link[=plotBar]{plotBar()}},
\code{\link[=plotDensBox]{plotDensBox()}},
\code{\link[=plotDot]{plotDot()}},
\code{\link[=plotPropCI]{plotPropCI()}}
}
\concept{descriptive-statistics}
\concept{graphics}
\concept{plot.univariate}
