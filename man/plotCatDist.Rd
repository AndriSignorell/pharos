% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCatDist.R
\name{plotCatDist}
\alias{plotCatDist}
\title{Categorical Distribution Plot}
\usage{
plotCatDist(
  x,
  type = c("both", "freq", "perc"),
  ecdf = FALSE,
  col = "grey80",
  border = FALSE,
  maxlablen = 25,
  maxcats = NULL,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{a factor, character vector, or table of counts.}

\item{type}{character; one of \code{"both"}, \code{"freq"}, \code{"perc"}.
Controls whether absolute frequencies, relative frequencies, or both
are displayed.}

\item{ecdf}{logical; if \code{TRUE}, cumulative proportions are shown
instead of simple relative frequencies.}

\item{col}{fill color for bars.}

\item{border}{logical; draw borders around bars.}

\item{maxlablen}{integer; maximum length of category labels before truncation.}

\item{maxcats}{optional maximum number of categories to display (truncates if exceeded).}

\item{main}{plot title.}

\item{...}{further graphical parameters passed to \code{par()}.}
}
\value{
Invisibly returns a list with frequencies and proportions.
}
\description{
Visualizes the distribution of a categorical variable using horizontal
bar plots of absolute and relative frequencies. Optionally, cumulative
proportions (ECDF-style) can be displayed.
}
\details{
The function produces horizontal bar plots:
\itemize{
\item Absolute frequencies (counts)
\item Relative frequencies (percentages) or cumulative proportions
}

If \code{type = "both"}, both views are shown side by side.

Long labels are truncated, and large category sets can be limited via
\code{maxcats}.
}
\examples{
# Basic usage
x <- factor(sample(letters[1:5], 100, TRUE))
plotCatDist(x)

# Only proportions
plotCatDist(x, type = "perc")

# With cumulative distribution
plotCatDist(x, ecdf = TRUE)

# Many categories (truncation)
x2 <- factor(sample(letters, 200, TRUE))
plotCatDist(x2, maxcats = 10)

}
\seealso{
Other plot.distribution: 
\code{\link{plotECDF}()},
\code{\link{plotFdist}()},
\code{\link{plotProbDist}()},
\code{\link{plotQQ}()}
}
\concept{descriptive-statistics}
\concept{factor-handling}
\concept{graphics}
\concept{plot.distribution}
