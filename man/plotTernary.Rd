% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTernary.R
\name{plotTernary}
\alias{plotTernary}
\title{Ternary Plot}
\usage{
plotTernary(
  x,
  y = NULL,
  z = NULL,
  ...,
  add = FALSE,
  col = NULL,
  pch = 16,
  cex = 1,
  grid = NA,
  lbl = NULL,
  main = "",
  xlim = c(-1, 1),
  ylim = c(-0.5, 1)
)
}
\arguments{
\item{x}{A numeric vector, matrix, or data frame. If \code{y} and \code{z}
are provided, \code{x}, \code{y}, and \code{z} are combined into a matrix.
Otherwise, \code{x} must contain exactly three columns.}

\item{y}{Optional numeric vector for the second component.}

\item{z}{Optional numeric vector for the third component.}

\item{...}{Additional graphical parameters passed to \code{par()}.}

\item{add}{Logical; if \code{TRUE}, adds to an existing plot.}

\item{col}{Point color(s).}

\item{pch}{Plotting symbol.}

\item{cex}{Point size.}

\item{grid}{Logical, \code{NA}, or list controlling the ternary grid.}

\item{lbl}{Character vector of length 3 specifying axis labels.}

\item{main}{Plot title.}

\item{xlim, ylim}{Plot limits (usually left at defaults).}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Draws a ternary plot for compositional data consisting of three non-negative
components per observation. Each row is interpreted as a composition and
internally rescaled to sum to 1 if necessary.
}
\details{
A ternary plot represents three-part compositions on an equilateral triangle.
Each observation \eqn{(x_1, x_2, x_3)} is mapped to 2D coordinates using
barycentric transformation. If rows do not sum to 1, they are automatically
normalized with a warning.

Graphical elements such as grids are controlled via the unified plot
design system using \code{bedrock::callIf()} and \code{.theme()}.
}
\examples{
set.seed(1)
x <- runif(100)
y <- runif(100)
z <- runif(100)

plotTernary(x, y, z)

# matrix input
M <- cbind(x, y, z)
plotTernary(M, lbl = c("A", "B", "C"))

}
\seealso{
\code{\link{plotDens}}, \code{\link{plotRidge}}

Other plot.special:
\code{\link[=plotArea]{plotArea()}},
\code{\link[=plotBinaryTree]{plotBinaryTree()}},
\code{\link[=plotCirc]{plotCirc()}},
\code{\link[=plotFun]{plotFun()}},
\code{\link[=plotMiss]{plotMiss()}},
\code{\link[=plotPolar]{plotPolar()}},
\code{\link[=plotTreemap]{plotTreemap()}},
\code{\link[=plotWeb]{plotWeb()}}
}
\concept{geometry}
\concept{graphics}
\concept{plot.special}
