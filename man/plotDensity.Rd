% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotDensity.R
\name{plotDensity}
\alias{plotDensity}
\alias{plotDensity.default}
\alias{plotDensity.formula}
\title{Grouped Density Plot}
\usage{
\method{plotDensity}{default}(
  x,
  ...,
  add = FALSE,
  bw = "nrd0",
  col = NULL,
  lwd = 2,
  lty = 1,
  fill = FALSE,
  grid = NA,
  main = "",
  xlab = "",
  ylab = "density",
  xlim = NULL,
  ylim = NULL,
  stamp = NULL
)

\method{plotDensity}{formula}(
  formula,
  data = NULL,
  subset,
  na.action = na.omit,
  ...,
  add = FALSE,
  bw = "nrd0",
  col = NULL,
  lwd = 2,
  lty = 1,
  fill = FALSE,
  grid = NA,
  main = "",
  xlab = "",
  ylab = "density",
  xlim = NULL,
  ylim = NULL,
  stamp = NULL
)
}
\arguments{
\item{x}{A numeric vector, or a list of numeric vectors representing groups.}

\item{...}{Additional graphical parameters passed to \code{par()}.}

\item{add}{Logical; if \code{TRUE}, adds to an existing plot.}

\item{bw}{Bandwidth selector passed to \code{\link[stats]{density}}.}

\item{col}{Line color(s) for the density curves.}

\item{lwd}{Line width(s).}

\item{lty}{Line type(s).}

\item{fill}{Logical or list; if \code{TRUE}, fills the area under each density.}

\item{grid}{Logical, \code{NA}, or list controlling background grid.}

\item{main, xlab, ylab}{Plot labels.}

\item{xlim, ylim}{Axis limits.}

\item{stamp}{Optional stamp passed to \code{.withGraphicsState()}.}

\item{formula}{A formula of the form \code{y ~ group}.}

\item{data}{Optional data frame.}

\item{subset}{Optional subset expression.}

\item{na.action}{Function to handle missing values.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Draws kernel density estimates for one or more groups as line plots.
Groups can be supplied either as a list of numeric vectors or via a
formula interface of the form \code{y ~ group}.
}
\details{
For each group, a kernel density estimate is computed using
\code{\link[stats]{density}}. The resulting curves are drawn as lines,
optionally with filled areas under the curve.

Graphical elements such as grids are controlled via the unified plot
design system using \code{.callIf()} and \code{.theme()}.
}
\examples{
set.seed(1)
x <- rnorm(100)
y <- rnorm(100, 1)

# default interface
plotDensity(x, y)

# grouped via list
plotDensity(list(A = x, B = y), fill = TRUE)

# formula interface
df <- data.frame(
  value = c(x, y),
  group = rep(c("A", "B"), each = 100)
)

plotDensity(value ~ group, data = df, fill = TRUE)

}
\seealso{
\code{\link[stats]{density}}
}
