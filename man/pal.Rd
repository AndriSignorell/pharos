% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pal.R
\name{pal}
\alias{pal}
\alias{plot.Palette}
\title{Get a Color Palette}
\usage{
pal(name, n = NA, opacity = 1)

\method{plot}{Palette}(x, cex = 2.5, border = "grey70", ...)
}
\arguments{
\item{name}{character or integer. Palette name (full match via
\code{\link[base]{match.arg}}) or index into \code{\link{palNames}()}.
If missing, returns the palette named in the active theme
(\code{getTheme()$palette}, see \link{theme}).}

\item{n}{integer, number of colors to return. Default \code{NA} returns
the colors as contained in the palette.}

\item{opacity}{numeric in \eqn{[0, 1]}, opacity. Default \code{1} (opaque).
Applied via \code{\link[grDevices]{adjustcolor}}.}

\item{x}{palette object to be plotted.}

\item{cex}{character extension.}

\item{border}{color for the border.}

\item{...}{further params.}
}
\value{
a character vector of \code{n} hex color codes of class
\code{c("palette", "character")} with a \code{"name"} attribute.
}
\description{
Returns \code{n} colors from a named palette. All palettes always return
exactly \code{n} colors regardless of their base size:
\itemize{
\item \code{n < length(base)}: evenly spaced sample for maximum contrast
\item \code{n = length(base)}: returned as-is
\item \code{n > length(base)}: interpolated via
\code{\link[grDevices]{colorRampPalette}}
}
}
\examples{
# default palette from options
pal()

# discrete palette — 3 maximally contrasting colors
pal("dark", n = 3)

# continuous gradient — 50 colors
pal("red-white-blue-2", n = 50)

# by index
pal(1, n = 10)

# with transparency
pal("helsana", n = 5, opacity = 0.5)

# show all names
palNames()

}
\seealso{
\code{\link{palNames}}, \code{\link[grDevices]{colorRampPalette}},
\code{\link[grDevices]{adjustcolor}}

Other color.palettes:
\code{\link[=hcol]{hcol()}},
\code{\link[=palNames]{palNames()}}
}
\concept{color}
\concept{color.palettes}
\concept{palette}
