% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.Lc.R
\name{plot.Lc}
\alias{plot.Lc}
\alias{lines.Lc}
\alias{points.Lc}
\alias{lines.LcList}
\alias{points.LcList}
\alias{plot.LcList}
\title{Plot Methods for Lorenz Curve Objects}
\usage{
\method{plot}{Lc}(
  x,
  main = "Lorenz curve",
  xlab = "p",
  ylab = "L(p)",
  xlim = NULL,
  ylim = NULL,
  general = FALSE,
  col = NULL,
  lwd = 2,
  lty = 1,
  pch = NULL,
  grid = FALSE,
  box = TRUE,
  stamp = NULL,
  ...
)

\method{lines}{Lc}(x, general = FALSE, col = NULL, lwd = 2, lty = 1, cbandArgs = NA, ...)

\method{points}{Lc}(x, general = FALSE, pch = 16, col = NULL, ...)

\method{lines}{LcList}(x, col = NULL, ...)

\method{points}{LcList}(x, col = NULL, ...)

\method{plot}{LcList}(x, col = NULL, ...)
}
\arguments{
\item{x}{Object of class \code{"Lc"} or \code{"Lclist"}.}

\item{main, xlab, ylab}{Main title and axis labels passed to
\code{\link[graphics]{plot.default}()}.}

\item{xlim, ylim}{Numeric vectors of length 2 giving axis limits.}

\item{general}{Logical.  If \code{TRUE}, the generalized Lorenz curve
(scaled by the mean) is displayed instead of the standard curve.
Default is \code{FALSE}.}

\item{col}{Color(s) for the curve(s).  For \code{"LcList"} methods,
recycled over groups; defaults to \code{1:k}.}

\item{lwd}{Line width.  Default is \code{2}.}

\item{lty}{Line type.  Default is \code{1}.}

\item{pch}{Plotting symbol.  If \code{NULL} (default in
\code{plot.Lc()}), no points are drawn.}

\item{grid}{Logical or list.  If \code{TRUE} or a list, a grid is drawn
before the curve.  A list is forwarded as arguments to
\code{\link[graphics]{grid}()}.}

\item{box}{Logical.  If \code{TRUE} (default), a box is drawn around the
plot area.}

\item{stamp}{Optional character string or list.  Passed to the aurora
graphics framework for plot annotation.}

\item{...}{Further graphical arguments passed to the underlying
\code{\link[graphics]{lines}()}, \code{\link[graphics]{points}()}, or
\code{\link[graphics]{plot.default}()} call.}

\item{cbandArgs}{\code{NA} to suppress the confidence band (default), or
a list of arguments passed to \code{\link[DescToolsX]{predict.Lc}()} to
control bootstrap confidence intervals.}
}
\value{
All methods return the input object \code{x} invisibly.
}
\description{
Visualize objects of class \code{"Lc"} and \code{"LcList"} returned by
\code{\link[DescToolsX]{lc}()}.  The \code{plot()} method draws a new
Lorenz curve plot including the line of perfect equality; \code{lines()}
and \code{points()} add to an existing plot.
}
\details{
For \code{"LcList"} objects (grouped Lorenz curves), \code{plot()} draws
the first group and overlays the remaining groups with \code{lines()}.
Colors cycle automatically when \code{col} is not supplied.

The confidence band in \code{lines.Lc()} is drawn via \code{cbandArgs}.
Pass a list of arguments to \code{\link[DescToolsX]{predict.Lc}()} to
control the bootstrap (e.g. \code{cbandArgs = list(conf.level = 0.90,
n = 500)}).  Set \code{cbandArgs = NA} (default) to suppress the band.
}
\seealso{
\code{\link[DescToolsX]{lc}()} for computing the Lorenz curve,
\code{\link[DescToolsX]{predict.Lc}()} for bootstrap confidence
intervals, \code{\link[DescToolsX]{gini}()} for the Gini coefficient.
}
\concept{graphics}
\concept{inequality}
