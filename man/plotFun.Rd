% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotFun.R
\name{plotFun}
\alias{plotFun}
\title{Plot Mathematical Functions}
\usage{
plotFun(
  expr,
  main = "",
  xlab = "",
  ylab = "",
  xlim = NULL,
  ylim = NULL,
  from = 0,
  to = 1,
  n = 500,
  args = NULL,
  col = 1,
  lwd = 1,
  lty = 1,
  grid = NA,
  add = FALSE,
  ...
)
}
\arguments{
\item{expr}{Expression defining the function. Either a formula (\code{y ~ f(x)}),
a polar formula (\code{r ~ f(phi)}), or a list of two formulas for parametric plots.}

\item{main}{Character. Main title of the plot.}

\item{xlab}{Character. Label for x-axis.}

\item{ylab}{Character. Label for y-axis.}

\item{xlim}{Numeric vector of length 2 defining x-axis limits.}

\item{ylim}{Numeric vector of length 2 defining y-axis limits.}

\item{from}{Numeric. Lower bound of the parameter domain.}

\item{to}{Numeric. Upper bound of the parameter domain.}

\item{n}{Integer. Number of evaluation points.}

\item{args}{Named list of additional parameters used in the function expression.}

\item{col}{Color of the line.}

\item{lwd}{Line width.}

\item{lty}{Line type.}

\item{grid}{Logical, \code{NA}, or list. Controls grid display.}

\item{add}{Logical. If \code{TRUE}, adds to existing plot.}

\item{...}{Additional graphical parameters passed to \code{par()}.}
}
\value{
Invisibly returns a list with components \code{x} and \code{y}.
}
\description{
Flexible plotting of mathematical functions in Cartesian, polar, and parametric form.
}
\details{
The function automatically detects the type of input:
\itemize{
\item \strong{Cartesian:} \code{y ~ f(x)}
\item \strong{Polar:} \code{r ~ f(phi)} or \code{r ~ f(theta)}
\item \strong{Parametric:} \code{list(x ~ f(t), y ~ g(t))}
}

Additional parameters can be passed via \code{args}.

The plotting is performed using base graphics with a fixed aspect ratio (\code{asp = 1}),
ensuring geometrically correct representations.

For Cartesian functions, \code{xlim} defaults to \code{c(from, to)}.
For polar and parametric plots, limits are derived from the data.
}
\examples{
par(mfrow = c(3,4))

# --- basic -------------------------------------------------
plotFun(y ~ x^2)

# --- Cartesian leaf ----------------------------------------
plotFun(
  list(
    x ~ 3*2*z / (z^3 + 1 + 0.1),
    y ~ 3*2*z^2 / (z^3 + 1)
  ),
  from = -10, to = 10,
  col = "magenta", asp = 1, lwd = 2
)

# --- damped oscillations -----------------------------------
plotFun(y ~ 3*exp(-x/5)*sin(4*x),
        from = 0, to = 10,
        col = "green")

plotFun(y ~ 3*exp(-x/5)*sin(6*x),
        from = 0, to = 10,
        col = "darkgreen", add = TRUE)

# --- cardioid (polar) --------------------------------------
plotFun(r ~ 2*(1 + cos(phi)),
        from = 0, to = 2*pi)

# --- heart curve (parametric) -------------------------------
plotFun(
  list(
    x ~ 13*cos(t) - 5*cos(2*t) - 2*cos(3*t) - cos(4*t),
    y ~ 16*sin(t)^3
  ),
  from = 0, to = 2*pi,
  col = "red", lwd = 2
)

# --- polar flower ------------------------------------------
plotFun(r ~ 6*sin(2*phi)*cos(2*phi),
        from = 0, to = 2*pi,
        col = "orange")

# --- astroid -----------------------------------------------
plotFun(
  list(
    x ~ 2*cos(t)^3,
    y ~ 2*sin(t)^3
  ),
  from = 0, to = 2*pi,
  col = "red", lwd = 3
)

# --- lemniscate --------------------------------------------
plotFun(r ~ (2*cos(2*phi))^2,
        from = 0, to = 2*pi,
        col = "darkblue")

# --- cycloid -----------------------------------------------
plotFun(
  list(
    x ~ 0.5*(t - sin(t)),
    y ~ 0.5*(1 - cos(t))
  ),
  from = 0, to = 30,
  col = "orange"
)

# --- involute ----------------------------------------------
plotFun(
  list(
    x ~ 0.2*(cos(t) + t*sin(t)),
    y ~ 0.2*(sin(t) - t*cos(t))
  ),
  from = 0, to = 50,
  col = "brown"
)

# --- Lissajous ---------------------------------------------
plotFun(
  list(
    x ~ sin(t),
    y ~ sin(2*t)
  ),
  from = 0, to = 2*pi,
  col = "blue", lwd = 2
)

# --- parameter sweep ---------------------------------------
for(a in 1:3) {
  plotFun(y ~ sin(a*x),
          args = list(a = a),
          from = 0, to = 2*pi,
          add = (a != 1),
          col = a)
}

# --- polar sine --------------------------------------------
plotFun(r ~ sin(3*phi),
        from = 0, to = pi,
        col = "deeppink4", lwd = 2)

# --- ripple ------------------------------------------------
plotFun(r ~ 1 + 0.1*sin(10*phi),
        from = 0, to = 2*pi,
        col = "deeppink4")

}
\seealso{
Other plot.special:
\code{\link[=plotArea]{plotArea()}},
\code{\link[=plotBinaryTree]{plotBinaryTree()}},
\code{\link[=plotCirc]{plotCirc()}},
\code{\link[=plotMiss]{plotMiss()}},
\code{\link[=plotPolar]{plotPolar()}},
\code{\link[=plotTernary]{plotTernary()}},
\code{\link[=plotTreemap]{plotTreemap()}},
\code{\link[=plotWeb]{plotWeb()}}
}
\concept{graphics}
\concept{mathematics}
\concept{plot.special}
