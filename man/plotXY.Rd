% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotXY.R
\name{plotXY}
\alias{plotXY}
\alias{plotXY.default}
\alias{plotXY.formula}
\title{Scatterplot with Optional Smooth Lines}
\usage{
plotXY(x, ...)

\method{plotXY}{default}(
  x,
  y,
  main = "",
  xlab = "",
  ylab = "",
  xlim = NULL,
  ylim = NULL,
  col = "grey50",
  bg = addAlpha("grey80"),
  pch = 21,
  cex = 1.1,
  grid = TRUE,
  lm = TRUE,
  loess = TRUE,
  legend = TRUE,
  ...
)

\method{plotXY}{formula}(
  formula,
  data,
  subset,
  na.action = na.omit,
  main = "",
  xlab = "",
  ylab = "",
  xlim = NULL,
  ylim = NULL,
  col = "grey50",
  bg = addAlpha("grey80"),
  pch = 21,
  cex = 1.1,
  grid = TRUE,
  lm = TRUE,
  loess = TRUE,
  legend = TRUE,
  ...
)
}
\arguments{
\item{x}{numeric vector of x-values, or a formula of the form \code{y ~ x}.}

\item{...}{further graphical parameters passed to \code{par()} via the
internal framework.}

\item{y}{numeric vector of y-values (ignored if a formula is used).}

\item{main}{main title of the plot.}

\item{xlab}{label for the x-axis.}

\item{ylab}{label for the y-axis.}

\item{xlim}{numeric vector of length 2; x-axis limits. If \code{NULL}
(default), the range of \code{x} is used.}

\item{ylim}{numeric vector of length 2; y-axis limits. If \code{NULL}
(default), the range of \code{y} is used.}

\item{col}{color of the points. Default: \code{"grey50"}.}

\item{bg}{background (fill) color of the points. Default: a transparent
\code{"grey80"}.}

\item{pch}{plotting character. Default: \code{21} (filled circle).}

\item{cex}{character expansion factor for points. Default: \code{1}.}

\item{grid}{controls drawing of the background grid.
Can be:
\itemize{
\item \code{TRUE}: draw grid with default settings
\item \code{FALSE}, \code{NULL}, or \code{NA}: suppress grid
\item a named list: arguments passed to \code{\link[graphics]{grid}}
}}

\item{lm}{controls drawing of the linear regression line.
Can be:
\itemize{
\item \code{TRUE}: draw with default settings
\item \code{FALSE}, \code{NULL}, or \code{NA}: suppress
\item a named list: arguments passed to \code{\link[graphics]{lines}},
e.g. \code{list(col = "blue", lwd = 2)}
}}

\item{loess}{controls drawing of the locally weighted regression line.
Can be:
\itemize{
\item \code{TRUE}: draw with default settings
\item \code{FALSE}, \code{NULL}, or \code{NA}: suppress
\item a named list: arguments passed to \code{\link[graphics]{lines}},
e.g. \code{list(col = "red", lty = "dashed")}
}}

\item{legend}{controls drawing of the legend.
Can be:
\itemize{
\item \code{TRUE}: draw with default settings (position \code{"topright"})
\item \code{FALSE}, \code{NULL}, or \code{NA}: suppress
\item a named list: arguments passed to \code{\link[graphics]{legend}},
e.g. \code{list(x = "bottomleft")}
}
The legend is only drawn when at least one of \code{lm} or \code{loess}
is active.}

\item{formula}{a formula of the form \code{y ~ x}.}

\item{data}{an optional data frame containing variables in the formula.}

\item{subset}{optional expression indicating which observations to use.}

\item{na.action}{a function specifying how missing values are handled.
Defaults to \code{na.omit}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Draws a scatterplot of two numeric variables with optional linear and
locally weighted regression lines, and an optional legend.
}
\details{
Optional plot components (\code{grid}, \code{lm}, \code{loess},
\code{legend}) follow \code{\link[bedrock]{callIf}} semantics:
\itemize{
\item \code{TRUE}: draw with defaults
\item \code{FALSE}, \code{NULL}, or \code{NA}: suppress component
\item named list: customize component arguments
}
}
\examples{
\dontrun{
plotXY(temperature ~ delivery_min, bedrock::Pizza,
       main = "Temperature vs. Delivery Time")

# Suppress loess, customize lm line
plotXY(temperature ~ delivery_min, bedrock::Pizza,
       lm    = list(col = "darkred", lwd = 2),
       loess = FALSE)
}

}
\seealso{
\code{\link[graphics]{plot}},
\code{\link[stats]{lm}},
\code{\link[stats]{loess}},
\code{\link[bedrock]{callIf}}
}
\concept{graphics}
\concept{regression}
\concept{scatterplot}
\concept{topic.plots}
